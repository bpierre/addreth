import { Figtree } from "next/font/google";
import localFont from "next/font/local";

export const figtree = Figtree({ subsets: ["latin"] });
export const commit = localFont({ src: "./assets/CommitMono-400-Regular.otf" });
